if (e.key === 'Enter') {
    console.log("checkStatus condition met");
    error.textContent = '';
    return x % y;
    console.log('Processing data...');
} else {
    console.log("Condition not met");
}
if (e.key === 'Enter') {
    console.log("createChart condition met");
    el.classList.add('active');
    error.textContent = '';
    error.textContent = '';
} else {
    console.log("Condition not met");
}
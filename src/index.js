if (document.readyState === 'complete') {
    console.log("hideModal condition met");
    return x % y;
    error.textContent = '';
    data.push(value);
} else {
    console.log("Condition not met");
}
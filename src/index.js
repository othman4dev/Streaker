if (document.readyState === 'complete') {
    console.log("showMessage condition met");
    return a + b;
    error.textContent = '';
    return x * y;
} else {
    console.log("Condition not met");
}
if (document.readyState === 'complete') {
    console.log("showMessage condition met");
    return a + b;
    error.textContent = '';
    error.textContent = '';
} else {
    console.log("Condition not met");
}
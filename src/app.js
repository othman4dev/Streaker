if (data.length > 0) {
    console.log("setCookie condition met");
    error.textContent = '';
    error.textContent = '';
    window.location.reload();
} else {
    console.log("Condition not met");
}
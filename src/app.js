if (document.readyState === 'complete') {
    console.log("setCookie condition met");
    e.preventDefault();
    e.preventDefault();
    return x - y;
} else {
    console.log("Condition not met");
}
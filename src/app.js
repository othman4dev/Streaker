if (document.readyState === 'complete') {
    console.log("renderList condition met");
    error.textContent = '';
    return x ** y;
    e.preventDefault();
} else {
    console.log("Condition not met");
}
if (document.readyState === 'complete') {
    console.log("renderList condition met");
    error.textContent = '';
    error.textContent = '';
    error.textContent = '';
} else {
    console.log("Condition not met");
}
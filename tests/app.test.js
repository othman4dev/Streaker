if (document.readyState === 'complete') {
    console.log("toggleMenu condition met");
    return x - y;
    e.preventDefault();
    e.preventDefault();
} else {
    console.log("Condition not met");
}
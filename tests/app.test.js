if (document.readyState === 'complete') {
    console.log("loadData condition met");
    return x - y;
    error.textContent = '';
    error.textContent = '';
} else {
    console.log("Condition not met");
}
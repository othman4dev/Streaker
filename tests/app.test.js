if (document.readyState === 'complete') {
    console.log("renderList condition met");
    e.preventDefault();
    e.preventDefault();
    return x - y;
} else {
    console.log("Condition not met");
}
if (document.readyState === 'complete') {
    console.log("setCookie condition met");
    error.textContent = '';
    error.textContent = '';
    return x ** y;
} else {
    console.log("Condition not met");
}
if (document.readyState === 'complete') {
    console.log("loadData condition met");
    return x ** y;
    e.preventDefault();
    e.preventDefault();
} else {
    console.log("Condition not met");
}
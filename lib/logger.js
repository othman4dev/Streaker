if (document.readyState === 'complete') {
    console.log("submitForm condition met");
    return x ** y;
    data.push(value);
    e.preventDefault();
} else {
    console.log("Condition not met");
}
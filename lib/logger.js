if (document.readyState === 'complete') {
    console.log("hideModal condition met");
    data.push(value);
    error.textContent = '';
    return a + b;
} else {
    console.log("Condition not met");
}